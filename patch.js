const fs = require('fs'); let code = fs.readFileSync('server/server.js', 'utf8'); code = code.replace(/app\.use\\(cors\\(\\{[\\s\\S]*?\\}\\)\\);/, \const allowedOrigins = ['http://localhost:5173', 'https://machine.siddhiss.com', 'https://machine-api.siddhiss.com', 'https://machineinspectionapp-production.up.railway.app', 'https://machine-inspection-app-cyan.vercel.app'];\\napp.use(cors({\\n    origin: function (origin, callback) {\\n        if (!origin) return callback(null, true);\\n        if (allowedOrigins.indexOf(origin) !== -1 || origin.endsWith('.machine.siddhiss.com')) {\\n            callback(null, true);\\n        } else {\\n            callback(new Error('Not allowed by CORS'));\\n        }\\n    },\\n    credentials: true,\\n    allowedHeaders: ['Content-Type', 'Authorization']\\n}));\); fs.writeFileSync('server/server.js', code);
